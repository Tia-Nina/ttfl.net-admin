{
  "name": "api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --var COOKIE_DOMAIN:localhost --var PUBLIC_AUTH_ORIGIN:http://localhost:8787 --var PUBLIC_HUB_ORIGIN:http://localhost:5173",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init:local": "wrangler d1 execute ttfl_hub --local --file=schema.sql && wrangler d1 execute ttfl_hub --local --file=seed.sql",
    "db:init:remote": "wrangler d1 execute ttfl_hub --remote --file=schema.sql && wrangler d1 execute ttfl_hub --remote --file=seed.sql"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.2.2",
    "hono": "^4.10.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260924.1",
    "typescript": "~6.0.2",
    "wrangler": "^4.46.0"
  }
}
